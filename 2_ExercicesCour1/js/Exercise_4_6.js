var tableau = [];

tableau.push ("pile"); //[>pile>]
tableau.push ("face");
tableau.push ("pile");
tableau.push ("pile");
tableau.push ("face");
tableau.push ("pile");
tableau.push ("pile");
tableau.push ("face");
tableau.push ("pile");
tableau.push ("pile");

for (var i=0; i< tableau.length; i++) {
    console.log (tableau[i]) ;

}